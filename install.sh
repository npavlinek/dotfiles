#!/usr/bin/env sh

ln -s "$PWD/gitconfig" "$HOME/.gitconfig"
ln -s "$PWD/hgrc" "$HOME/.config/hg/hgrc"
ln -s "$PWD/openbox" "$HOME/.config/openbox"
ln -s "$PWD/vim" "$HOME/.vim"
ln -s "$PWD/xinitrc" "$HOME/.xinitrc"
